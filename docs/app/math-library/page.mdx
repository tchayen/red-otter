import { Api } from "./Api";
import { getMetadata } from "../getMetadata";
export const metadata = getMetadata("Math Library");

# Math Library

Since the whole project is entirely zero-dependency, it comes with its own math library.

## API

<Api />
